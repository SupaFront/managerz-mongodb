const addNote = require('./addNote');
const editNote = require('./editNote');
const getNotes = require('./getNotes');
const removeNote = require('./removeNote');

module.exports = { addNote, editNote, getNotes, removeNote };
